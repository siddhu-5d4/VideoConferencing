{
    "name": "zoom",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node server.js",
        "build": "npm run build:ejs && npm run build:css && npm run build:js",
        "build:ejs": "echo \"No EJS build steps needed for this project\"",
        "build:css": "mkdir -p public/dist && cp public/style.css public/dist/",
        "build:js": "mkdir -p public/dist && cp public/script.js public/dist/"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "ejs": "^3.1.3",
        "express": "^4.18.2",
        "firebase": "^10.3.0",
        "nodemon": "^2.0.4",
        "peer": "^0.5.3",
        "socket.io": "^2.3.0",
        "uuid": "^8.3.0"
    }
}
